"use client";

import { DocumentBrand } from "@/features/documents/components/document-brand";

/** @deprecated Prefer DocumentBrand — kept for compatibility. */
export function TenantDocumentBrand() {
  return <DocumentBrand />;
}
